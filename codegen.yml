schema: 'src/schema/**/*.graphql'
generates:
    ./generated/schema.ts:
        config:
            useIndexSignature: true
            contextType: ../src/dataSources#Context
            strictScalars: true
            scalars:
              Date: '../src/scalarTypes#DateScalar'
        plugins:
            - typescript
            - typescript-resolvers
